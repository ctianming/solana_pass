# Relayer configuration (example)
RPC_ENDPOINT=https://api.devnet.solana.com
# Optional Jito bundle URL or tip account
JITO_RELAY_URL=
FEEPAYER_SECRET_KEY_BASE58=
PARENT_OWNER_SECRET_KEY_BASE58=
# SAS public key / JWKs endpoint for verification
SAS_JWKS_URL=
SAS_DEV_BYPASS=false
# Rate limit config
RATE_LIMIT_MAX=60
# Optional Redis for nonce/rate limits (fallback to memory if empty)
REDIS_URL=
NONCE_TTL_SEC=600
DEDUPE_TTL_SEC=600
